import { resourceListFactory } from "../resource.js";

// Only add if you want nonzero specific amounts. The factory should take care of the rest.
export const playerResources = resourceListFactory(
/*     {
        scrap: 5000,
        ironOre: 1000,
        copperOre: 10000,
        iron: 10000,
        bit: 1000000
    } */
);